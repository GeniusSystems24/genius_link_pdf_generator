# Dart usage synchronization

The showcase no longer displays hand-written placeholder code for working PDF examples.

## Rules

- Workbench usage is generated from the destination's real `ShowcaseDemoKind`, file name, and background mode.
- The real builder class is extracted from the current repository and embedded in the usage panel.
- Reference pages only show code when a real implementation source is available.
- Placeholder names such as `MyDocument`, `InvoiceDocument`, and fake runtime ports are not used as UI usage examples.

Workbench destinations synchronized: **12**
Real reference sources synchronized: **2**

All registered workbench builder sources were extracted successfully.

Generated by `sync_dart_usage_with_generation.py`.
